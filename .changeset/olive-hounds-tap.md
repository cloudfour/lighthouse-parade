---
'lighthouse-parade': patch
---

Update `csv-parse` to 7.x and `csv-stringify` to 6.x, and drop the unused `csv` umbrella package. Aggregated report output is unchanged — the byte-for-byte fixture comparison passes against the new versions.
